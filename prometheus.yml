global:
  scrape_interval: 15s

rule_files:
  - "alert.rules"

scrape_configs:
  - job_name: 'aiops-agent'
    static_configs:
      - targets: ['app:8000']

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']
